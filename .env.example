# ============================================
# Exist AI Recruiter — Environment Variables
# ============================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit the .env file to version control.

# --------------------------------------------------
# Database (PostgreSQL) — EXTERNAL, PRE-CONFIGURED
# --------------------------------------------------
# The database is managed by your organization's infra team.
# Update these values with your actual database connection details.
# Example: Azure Database for PostgreSQL, AWS RDS, on-premise server, etc.

PGHOST=your-postgres-server.database.azure.com
PGUSER=recruiter
PGPASSWORD=CHANGE_THIS_STRONG_PASSWORD
PGDATABASE=recruiter_db
PGPORT=5432

# --------------------------------------------------
# API Server
# --------------------------------------------------
API_PORT=3001

# Host port for the frontend (default 80). Change if port 80 is taken.
FRONTEND_PORT=80

# --------------------------------------------------
# n8n Webhook URLs (pre-configured — change only if your n8n instance differs)
# --------------------------------------------------
# CV upload processing pipeline
N8N_CV_WEBHOOK_URL=https://workflow.exist.com.ph/webhook/vector-db-loader

# Job order sync (for embeddings)
N8N_JO_WEBHOOK_URL=https://workflow.exist.com.ph/webhook/job-order-webhook-path

# Email sending webhook
N8N_EMAIL_WEBHOOK_URL=https://workflow.exist.com.ph/webhook/81f944ac-1805-4de0-aec6-248bc04c535d

# --------------------------------------------------
# Webhook Callback
# --------------------------------------------------
# This URL must be reachable by n8n so it can POST processed CV results back.
# In production, set this to the actual public URL of your backend, e.g.:
#   https://recruiter.yourcompany.com/webhook-callback
WEBHOOK_CALLBACK_URL=http://localhost:3001/webhook-callback

# --------------------------------------------------
# Frontend (build-time only — baked into JS bundle)
# --------------------------------------------------
# When using the nginx reverse proxy in docker-compose, the frontend
# reaches the API through the same origin, so leave this empty or set to "/".
# Only change this if the API is hosted on a different domain.
VITE_API_URL=

# n8n chatbot webhook (called directly from the browser)
VITE_N8N_CHAT_WEBHOOK_URL=https://workflow.exist.com.ph/webhook/51c69627-4831-44a4-8d91-1824a7d38ebf
